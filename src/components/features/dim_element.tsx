import React, { FC } from 'react';
import { Spacer } from "../core/Containers";

export const DimElement: FC<any> = () => {
    return (
        <Spacer className="fixed index-10 flex min-h-screen w-full flex-col items-center justify-center bg-fullBlack bg-opacity-50"/>
    )
}
